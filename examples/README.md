# `examples/`

`sennit.config.example.yaml` — example `mock` server pointing at **`dist/fixtures/mock-upstream.js`** (run `npm run build` first).

Use: `npx sennit serve --config examples/sennit.config.example.yaml`.
